import 'package:flutter/material.dart';

/// HustleX Pro brand colors
abstract class AppColors {
  // Primary colors
  static const primary = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF9D97FF);
  static const primaryDark = Color(0xFF4A42DB);

  // Secondary colors
  static const secondary = Color(0xFF00D9A5);
  static const secondaryLight = Color(0xFF5EFFCE);
  static const secondaryDark = Color(0xFF00A67E);

  // Accent colors
  static const accent = Color(0xFFFF6B6B);
  static const accentLight = Color(0xFFFF9E9E);
  static const accentDark = Color(0xFFE63946);

  // Neutral colors
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF1F3F5);

  // Text colors
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6C757D);
  static const textTertiary = Color(0xFFADB5BD);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // Status colors
  static const success = Color(0xFF28A745);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFDC3545);
  static const info = Color(0xFF17A2B8);

  // Dark mode colors
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkSurfaceVariant = Color(0xFF2D2D2D);
}
