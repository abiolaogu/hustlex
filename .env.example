# =============================================================================
# HustleX Docker Compose Environment Configuration
# =============================================================================
# Copy this file to .env and configure all required secrets before starting services
# NEVER commit .env to version control
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your secure values
#   docker-compose up -d

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
ENVIRONMENT=development

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DB_NAME=hustlex
DB_USER=hustlex
# REQUIRED: Set a strong password (minimum 16 characters)
DB_PASSWORD=

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# REQUIRED: JWT secret for token signing (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# NEVER use * in production
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# -----------------------------------------------------------------------------
# Payment Gateway Webhooks
# -----------------------------------------------------------------------------
# Paystack webhook secret (from your Paystack dashboard)
PAYMENT_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Development Tools (Optional)
# -----------------------------------------------------------------------------
# pgAdmin
PGADMIN_EMAIL=admin@hustlex.ng
PGADMIN_PASSWORD=

# Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=

# RabbitMQ
RABBITMQ_USER=hustlex
RABBITMQ_PASSWORD=

# -----------------------------------------------------------------------------
# Generate Secure Passwords
# -----------------------------------------------------------------------------
# Run these commands to generate secure values:
#
# DB_PASSWORD:
#   openssl rand -base64 24
#
# JWT_SECRET:
#   openssl rand -base64 48
#
# PGADMIN_PASSWORD, GRAFANA_PASSWORD, RABBITMQ_PASSWORD:
#   openssl rand -base64 16
